node {
    stage('Example') {
        if (env.BRANCH_NAME == 'master') {
            echo 'I onlyt execute on the master branch'
        } else {
            echo 'I execute elsewhere'
        }
    }
}
